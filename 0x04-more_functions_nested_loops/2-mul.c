#include "main.h"

/**
 * mul - function that prints integers
 * @a: to be multiplied
 * @b: to be multiplied
 * Return: 0
 */
int mul(int a, int b)
{
	int mul;

	mul = a * b
	printf("%d multiplied with %d is\n", a, b, mul);

	return (mul);
}
