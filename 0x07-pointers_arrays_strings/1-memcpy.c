#include "main.h"

/**
 * memcpy - function that copies memory area
 * @n: number of bytes
 * @dest: pointer
 * @src: pointer
 * Return: dest string
 */

void main()
{
	int i;

	for (i = 0 ; src[i] != '\0' ; i++)
	{
		dest[i] = src [i];
	}
	return (dest);
}
